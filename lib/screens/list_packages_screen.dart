import 'package:flutter/material.dart';

class ListPackagesScreen extends StatelessWidget {
  const ListPackagesScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('List'),
    );
  }
}
